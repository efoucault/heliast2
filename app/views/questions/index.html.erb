<div id="notifications">
</div>


<!--  On itere sur les différentes catégorie -->
<div class="banner" style="background-image: linear-gradient(-225deg, rgba(17, 193, 193,0.7) 30%, rgba(227, 66, 104,0.6) 80%), url('https://images.thinkadvisor.com/contrib/content/uploads/sites/415/2018/02/Woman_Retail_Dresses_Shopping_MI600.jpg');">
  <div class="banner-content">
    <h1>Prenez des décisions <br/>pour des inconnus... </h1>
    <p>Et devenez l'Héliaste ultime</p>
  </div>
</div>
<div class="bg-light">
  <div class="container">
    <div class="tabs">
      <div class="category-card responsive-tab">
        <span id="all" class="tab">Tout</span>
        <%= link_to "", questions_path, class: 'category-card-link' %>
      </div>
      <% Category.all.each do |category| %>
          <!--  Chaque itération est une card et a une classe -->
          <div class="category-card responsive-tab">
            <% if category != nil %>
              <% if category.name == "Culture" %>
                <%= image_tag "badge-13.png", width: 40, class: 'tab', data: { type: category.name } %>
              <% elsif category.name == "Achats" %>
                <%= image_tag "badge-14.png", width: 40, class: "tab", data: { type: category.name }  %>
              <% elsif category.name == "Food" %>
                <%= image_tag "badge-15.png", width: 40, class: "tab", data: { type: category.name }  %>
              <% elsif category.name == "Travail" %>
                <%= image_tag "badge-16.png", width: 40, class: "tab", data: { type: category.name }  %>
              <% elsif category.name == "Look" %>
                <%= image_tag "badge-17.png", width: 40, class: "tab", data: { type: category.name }  %>
              <% elsif category.name == "Love" %>
                <%= image_tag "badge-18.png", width: 40, class: "tab", data: { type: category.name }  %>
              <% end %>
            <% end %>
            <%= link_to "", questions_path(query: "#{category.name}"), class: 'category-card-link' %>
          </div>
      <% end %>
    </div>
    <div class="row">
      <div class="col-6">
        <div class="questions">
          <%= render "questions/question_cards" %>
        </div>
      </div>
    </div>

  </div>
</div>
