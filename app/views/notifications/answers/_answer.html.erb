<!-- Bloc a activer si on veut afficher la liste de notifs dans la page historique  -->

<div class="notif-back <%= "unread" if !notification.read_at? %>">
  <div class="line-notif row.no-gutters">
        <div class="col-xs-2.no-gutters" style="margin-left: 0; text-align: left;">
          <% if notification.actor.photo.present? %>
              <%= cl_image_tag notification.actor.photo, class: 'avatar-notif-bordered' %>
          <% else %>
            <%= image_tag('https://www.limestone.edu/sites/default/files/user.png', class: 'avatar-notif-bordered') %>
          <%end%>
        </div>

        <div class="col-xs-8.no-gutters" style="text-align: left;">
          <%= link_to question_path(notification.notifiable.option.question) do %>
            <%= notification.actor.pseudo %>
            <%= notification.action %> à ta question
          <% end %>
          <p style="text-align: left!important;"><span class="time">Il y'a 1h</span></p>
        </div>

        <div class="col-xs-1.no-gutters" style="float: right; text-align: center;">
            <i class="fa fa-ellipsis-h" style="color: #9a888e;"></i>
        </div>
  </div>
</div>


<!-- TODO Bloc pour faire un lien sur la div global, css déjà en place mais ne fonctionne pas -->

<!-- <%= link_to "", question_path(notification.notifiable.option.question), class: 'notif-link-div' %> -->

<!-- Bloc a décommenter si on veut afficher la liste de notifs dans menu déroulant  -->

<!-- <li>
  <%= link_to question_path(notification.notifiable.option.question), class: "dropdown-item #{"unread" if !notification.read_at?}" do %>
    <%= notification.actor.pseudo %>
    <%= notification.action %> à ta question
  <% end %>

</li> -->

<!-- question_path(notification.notifiable.option.question) -->
