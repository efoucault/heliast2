<%= simple_form_for [@question, Option.new] do |f| %>
  <%= f.input :titre %>
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.submit "enregistrer l'option", class: "btn btn-success" %>
<% end %>
