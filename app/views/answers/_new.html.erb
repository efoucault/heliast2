<%= simple_form_for [@question, Answer.new] do |f| %>
  <%= f.hidden_field :option_id %>
  <%= f.input :reponse, label: false %>
  <%= f.submit "enregistrer la réponse", class: "btn btn-success btn-add" %>
<% end %>
